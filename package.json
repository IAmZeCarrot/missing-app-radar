{
  "name": "missing-app-radar",
  "version": "0.5.0",
  "description": "An evidence-backed dashboard for finding recurring software requests worth building.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "npx --yes http-server . -p 4173",
    "collect:hn": "node scripts/collect-hn.mjs",
    "collect:github": "node scripts/collect-github.mjs",
    "cluster": "node scripts/build-clusters.mjs",
    "refresh": "npm run collect:hn && npm run collect:github && npm run cluster",
    "test": "node --test",
    "check:syntax": "node scripts/check-syntax.mjs",
    "check": "npm run check:syntax && npm test"
  }
}
